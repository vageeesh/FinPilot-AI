# Agent registry keys — single source of truth
# Used by registry.py, lifecycle.py, and anywhere else that references agents by key.

BUDGET_AGENT = "budget_agent"
TAX_AGENT = "tax_agent"
INVESTMENT_AGENT = "investment_agent"
DEBT_AGENT = "debt_agent"
SQL_AGENT = "sql_agent"
